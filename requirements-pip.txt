# Minimal pip requirements for running SALSA training and evaluation
# Note: install PyTorch using the official selector for your CUDA version
# Example (CUDA 11.3):
#   pip install --extra-index-url https://download.pytorch.org/whl/cu113 torch==1.9.0+cu113 torchvision==0.10.0+cu113

numpy>=1.21
scipy>=1.7
pandas>=1.3
matplotlib>=3.3
seaborn>=0.11
scikit-learn>=0.24
joblib
tqdm
ipython
jupyter
matplotlib-inline
# PyTorch: install via official instructions for matching CUDA
# torch
# torchvision
# torchaudio (optional)

# Optional / helpful
pyyaml
astunparse
